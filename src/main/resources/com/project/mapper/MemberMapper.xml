<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.mapper.MemberMapper">

	<!-- 사용자 정의 resultMap 요소 정의 -->
	<resultMap type="Member" id="memberMap">
		<result property="userNo" column="user_no" />
		<result property="userId" column="user_id" />
		<result property="userPw" column="user_pw" />
		<result property="userName" column="user_name" />
		<result property="job" column="job" />
		<result property="coin" column="coin" />
		<result property="enabled" column="enabled" />
		<result property="regDate" column="reg_date" />
		<result property="updDate" column="upd_date" />
		<collection property="authList" resultMap="authMap">
		</collection>
	</resultMap>

	<resultMap type="MemberAuth" id="authMap">
		<result property="userNo" column="user_no" />
		<result property="auth" column="auth" />
	</resultMap>

	<!-- 등록 처리 -->
	<insert id="register">
		INSERT INTO
		member(user_no, user_id, user_pw,
		user_name, job)
		VALUES(member_seq.NEXTVAL, #{userId}, #{userPw},
		#{userName}, #{job})
	</insert>

	<!-- 그룹코드 목록 조회 -->
	<!-- 권한 생성 -->
	<insert id="createAuth">
		INSERT INTO member_auth(user_no, auth)
		VALUES(member_seq.CURRVAL, #{auth})
	</insert>

	<!-- 목록 조회 -->
	<select id="list" resultType="Member">
		SELECT user_no, user_id, user_pw,
		user_name,
		(SELECT code_name FROM code_detail WHERE group_code = 'A00'
		AND code_value = job) AS job,
		coin, reg_date FROM member ORDER BY
		reg_date DESC
	</select>

	<!-- 상세 조회 -->
	<select id="read" resultMap="memberMap">
		SELECT M.user_no, user_id, user_pw,
		user_name, job, coin, reg_date, upd_date, A.user_no, A.auth
		FROM member
		M LEFT OUTER JOIN member_auth A ON M.user_no = A.user_no
		WHERE
		M.user_no = #{userNo}
	</select>

	<!-- 권한 수정 -->
	<insert id="modifyAuth">
		INSERT INTO member_auth(user_no, auth)
		VALUES(#{userNo}, #{auth})
	</insert>

	<!-- 권한 삭제 -->
	<insert id="deleteAuth">
		DELETE FROM member_auth WHERE user_no = #{userNo}
	</insert>

	<!-- 수정 처리 -->
	<update id="modify">
		UPDATE member SET user_name =#{userName}, job =#{job}
		WHERE user_no = #{userNo}
	</update>

	<!-- 삭제 처리 -->
	<delete id="remove">
		DELETE FROM member WHERE user_no = #{userNo}
	</delete>

	<!-- 회원 테이블의 데이터 건수 조회 -->
	<select id="countAll" resultType="int">
		SELECT count(*) FROM member
	</select>
</mapper>




